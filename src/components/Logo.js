import React from "react";
import styles from '../styles/Header.module.css';

const Logo = () => {
  return (
    <div>
      <h1 className = {styles.logo}>Logo</h1>
    </div>
  );
};

export default Logo;